// jq 对象结点操作
$(jsObject); //将js对象转换成jq对象; 反之：var jsObject = jqObject[0]

jqObject.attr();  //设置属性 获取属性值

jqObject.removeClass(); //删除某个类名
jqObject.addClass(); //增加某个类名

jqObject.each(function (index, item) {} )
